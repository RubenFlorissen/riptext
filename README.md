# riptext

Cross-platform TUI text transformation app inspired by Boop semantics.

## Development (uv)

- Create and sync environment: `uv sync`
- Run: `uv run riptext`

This is an early scaffold. See AGENTS.MD for the build spec.
